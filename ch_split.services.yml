services:
  ch_split.ch_eligibility.subscriber:
    class: Drupal\ch_split\EventSubscriber\ContentHubEntityEligibilitySubscriber
    arguments: ['@config.factory']
    tags:
      - { name: event_subscriber }
